# gitconfig — dotfiles global git configuration
# Applied by dotfiles_install to ~/.gitconfig base
# Per-account identity is handled via includeIf blocks (set up by setup_identity)

[init]
    defaultBranch = main

[core]
    autocrlf = input
    excludesfile = ~/.gitignore_global

[pull]
    rebase = false

[push]
    autoSetupRemote = true

[fetch]
    prune = true
